# ProphetOps Setup Guide

The canonical setup guide is now in the project root:

```text
SETUP.md
```

Use that file for installation, local database setup, demo credentials, ZAP testing mode, and troubleshooting.

Quick reminder: the current capstone prototype uses SQLite at `database/database.sqlite`, so XAMPP is optional for normal testing.
